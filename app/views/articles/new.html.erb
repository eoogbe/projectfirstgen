<% provide(:title, "New Blog") %>
<% provide(:heading, "New blog") %>

<%= semantic_form_for article, html: { role: "form" } do |f| %>
  <%= f.inputs :title, :text %>
  <%= f.actions do %>
    <%= f.action :submit, button_html: { class: "btn btn-primary" } %>
  <% end %>
<% end %>
